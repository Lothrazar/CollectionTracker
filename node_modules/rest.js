function call(args,method,query_callback)
{  
    //console.log(args);
    //console.log(method);
    
    if(method == "GET")
    { 
        var table = args[0];
        
        var str_query = 'SELECT * from tracker.'+table;
        
        var mysql      = require('mysql');
     
        var connection = mysql.createConnection(
        {
          host     : 'localhost',
          user     : 'root',
          password : 'j1135#weep'
        });
          
        connection.connect(function(err)  {});
         
        connection.query(str_query , query_callback); 
        connection.end(); 
    }
    else if(method == "POST")
    {
        
        
    }
    //PUT // DELETE ?
}

exports.call = call;
